# LLM Walk-Through：从原理到实现，亲手解剖现代大语言模型

*本开题报告在经过初期调研后撰写，实现细节以最终成果为准*

## 一、项目概述

Andrej Karpathy 的 **nanoGPT** 用极简代码完整复现了 GPT-2，让初学者能在几个小时内感受一个小型 GPT 的训练全貌。**MiniMind** 则更进一步，在 26M 的极小规模上打通了从预训练、指令微调到偏好对齐的完整链路，真正做到了极致轻量。它们都解决了“跑通一个模型”的问题。

但跑通之后呢？学习者往往能拿到一条困惑度下降曲线，却很难回答：为什么 LLaMA 没有沿用 GPT-2 的架构，而要逐项引入 RoPE、RMSNorm、SwiGLU 和 GQA？每一处替换到底在解决什么工程瓶颈？带来了多大的实际收益？**LLM Walk-Through** 正是为回答这些“为什么”而生。

本项目定位为一部**以“理清技术路径、底层原理和工程细节”为核心的教学工具书**（或者说“可亲手编译的 LLM 教程”），而非一个单纯以跑分为目标的训练脚本。我们将模型拆解为可独立替换的模块，为每一个现代LLM的架构升级提供独立的验证实验。

需要说明的是，这种以理解为核心的目标，也让本项目对“极小模型上必须显著见效”这件事保持审慎。某些算法（如 SimPO）在小模型上位居末流，模型扩展到 7B 时却一跃成为最佳方案——算法效果的相对排名因规模发生了反转。这一结论同样适用于部分架构改进：它们在 60M 级别可能看不出明显优势，但这些设计选择背后的动机、理论分析和工程考量，在稍大一点的规模上都会变得清晰且重要。正因如此，本项目以 **“走通全流程，看全每一处设计取舍的理由”** 为首要任务，不强求所有结论在玩具尺度上立刻兑现，而是帮助学习者在恰当的实验范围内建立起完整的认知框架。

## 二、项目内容与技术选型

### 2.1 预训练架构模块

模型被解构为七个可独立替换的单元，每个单元同时实现经典方案与改良版本，学习者可在 60M–220M 参数规模上运行消融实验，设计实验直观对比不同设计选择带来的影响。

| 模块类别         | 拟实现的核心组件                                             |
| :--------------- | :----------------------------------------------------------- |
| 分词器           | BPE / WordPiece / BBPE，对比不同词表下中英文切割效率         |
| 位置编码         | Sinusoidal → ALiBi → RoPE → NTK 插值 → YaRN，验证长度外推能力 |
| 归一化层         | LayerNorm / RMSNorm / Pre‑LN vs Post‑LN，测试深层梯度稳定性  |
| 注意力机制       | MHA / MQA / GQA / MLA，搭载 FlashAttention‑2，对比显存占用与生成速度 |
| 前馈网络         | ReLU / GELU / SwiGLU / 稀疏 MoE，评估收敛速度与计算效率      |
| 残差连接与信息流 | 标准残差 → mHC/Attention Residuals，验证深层信号稳定性与信息选择性 |
| 显存缓存         | Naive KV Cache / PagedAttention / StreamingLLM，解决长生成 OOM |

### 2.2 后训练技术体系

预训练赋予模型语言能力，后训练则负责塑造其行为方式。项目将覆盖三条关键的后训练路线：

- **SFT（有监督微调）**：用高质量“指令–回答”样本教会模型对话格式与回答规范，是所有后训练的起点。
- **GRPO（组相对策略优化）**：无需奖励模型的在线强化学习，通过对同一问题采样一组回答并以组内相对优势替代绝对奖励，推动模型在数学、代码等推理任务中通过自我博弈实现能力跃升。
- **SimPO（简单偏好优化）**：以序列自身生成概率作为奖励信号，省去偏好优化中必须维护的参考模型，使训练更加轻量，同时在较大规模上展现出有竞争力的偏好对齐能力。

三条路线将对照同一个基座模型执行，以清晰展示“指令跟随”“审美偏好”和“逻辑推理”三类能力各自由哪一阶段的训练所塑造。

| 训练阶段 | 拟采用方法                  | 核心训练数据集                                               | 对应评测基准                                          |
| :------- | :-------------------------- | :----------------------------------------------------------- | :---------------------------------------------------- |
| 预训练   | 纯 PyTorch + DeepSpeed ZeRO | FineWeb-Edu（英文）、OpenCSG 中文语料、The Stack v2（代码）、MathPile（数学） | MMLU、HellaSwag、ARC、WinoGrande、WikiText-103（PPL） |
| SFT      | Full FT / LoRA / QLoRA      | OpenHermes-2.5（通用指令）、MetaMathQA（数学）、Magicoder-OSS（代码） | MT-Bench、IFEval                                      |
| 偏好对齐 | SimPO                       | UltraFeedback-Binarized（约 66K 偏好对）                     | AlpacaEval 2.0                                        |
| 推理强化 | GRPO                        | 基于编译/答案校验的在线采样环境（万级规模）                  | GSM8K、MATH、HumanEval、MBPP                          |

评测统一接入 HuggingFace **lm-evaluation-harness**，保证结果可复现。


## 三、项目规范与文档体系

项目遵循“**文档即代码，教程即实验**”的原则，将传统的静态 Markdown 文档全面升级为交互式的 **Jupyter Notebook (.ipynb)**。这种设计旨在消除“看懂了原理但跑不通代码”的断层感。

- **交互式技术底稿**：`docs/` 下的每个模块对应一个交互式笔记本。它不仅系统阐释问题背景、数学原理（LaTeX 公式）和改进思路，还包含可直接运行的 Python 代码块。学习者可以在阅读原理的同时，观察张量的维度变化、模型的参数分布或函数的输出结果。
- **源码高能导航（Deep Links）**：在 Notebook 关键位置嵌入指向 `core/` 下具体源码的精准软链接。链接不仅指向文件，更利用锚点技术直接定位至具体的起始行（如 `[CausalSelfAttention](../core/attention/mha.py#L48)`），实现“理论讲解 -> 实现细节”的无缝瞬移。
- **模块开发看板**：每个 Notebook 结尾均包含该模块的实验监控建议与演进路线。通过加载 `configs/` 中的 YAML 配置，学习者可以直观看到不同参数对模型行为的影响。
- **外部资源地图**：整合了论文、工程实现（如 nanoGPT, FlashAttention）以及高质量博客。文档不再是信息的孤岛，而是通往 LLM 广袤生态的入口。

## 四、项目核心价值

- **以“为什么”而非“怎么跑”为主线**：每个模块都配备原理说明和代码详解，目标是建立深层直觉，而非单纯跑完流水线。
- **模块完全解耦，可局部验证**：本地设备即可完成单个模块的消融实验，降低参与门槛。
- **预训练与后训练全景覆盖**：既理解模型如何被搭建，也看清它如何被调优，见证同一基座在不同训练范式下的能力分化。
- **尊重规模效应，不追求玩具验证一切**：以 walk‑through 的方式帮助学习者在合适的规模上建立完整认知，避免因模型规模过小而产生误判。

## 五、项目产出

- 一个带有丰富说明文档的gihub repo
- 一个或多个走过完整训练流程的1B级别大模型

项目地址：https://github.com/HenryNotTheKing/LLM-Walk-Through

